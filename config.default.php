<?php

/* PHublish configuration follows
return [
    // If you do not configure an absolute path, the script will take all .md files 
    // from the /article/  directory
    // 'source_directory' => '/path/to/articles/',
    // set the following to a public_html directory
    'destination_directory' => '/path/for/publication/'
  ];

